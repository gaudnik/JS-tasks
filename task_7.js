// write a function that return object containing 
// key and value: how many times letter appear in string.

function countLetter(word){
    const arr= []
      arr.push(word.split(""));
  
      return arr;
  }
  
  console.log(countLetter("zuza"));